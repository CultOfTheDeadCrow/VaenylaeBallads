---
phase: 01-wotlk-compatibility-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VaenylaeBard.toc
  - VaenylaeBard.lua
autonomous: true
requirements:
  - PORT-01
  - PORT-02
  - PORT-03
  - PORT-04
  - PORT-07

must_haves:
  truths:
    - "The 3.3.5a client loads VaenylaeBard without an out-of-date warning"
    - "ADDON_LOADED fires and initializes the addon (songs list populates)"
    - "Auto-playback timer advances each frame without Lua errors"
    - "Song line count checks use the # operator (no table.getn calls remain)"
    - "Timing residual subtraction prevents playback drift"
  artifacts:
    - path: "VaenylaeBard.toc"
      provides: "Interface version 30300"
      contains: "## Interface: 30300"
    - path: "VaenylaeBard.lua"
      provides: "OnEvent explicit signature"
      contains: "function(self, event, arg1)"
    - path: "VaenylaeBard.lua"
      provides: "OnUpdate explicit signature with elapsed"
      contains: "function(self, elapsed)"
    - path: "VaenylaeBard.lua"
      provides: "Residual subtraction timing reset"
      contains: "VB_playbackElapsed - effectiveDelay"
  key_links:
    - from: "VaenylaeBard.toc line 1"
      to: "WoW 3.3.5a client loader"
      via: "## Interface: 30300"
      pattern: "Interface: 30300"
    - from: "VaenylaeBard.lua line 200"
      to: "ADDON_LOADED event handler"
      via: "function(self, event, arg1) parameter binding"
      pattern: "OnEvent.*function\\(self, event, arg1\\)"
    - from: "VaenylaeBard.lua line 134"
      to: "playback timer"
      via: "function(self, elapsed) parameter binding"
      pattern: "OnUpdate.*function\\(self, elapsed\\)"
---

<objective>
Apply core runtime compatibility fixes: .toc Interface version bump, OnEvent and OnUpdate callback signatures, all five table.getn replacements, and the timing drift fix. These are the foundational changes — without them the addon fails to load (PORT-01), never initializes (PORT-02), produces Lua errors every frame during playback (PORT-03, PORT-04), and exhibits cumulative timing drift (PORT-07).

Purpose: Get the addon past the client loader gate and restore initialization and playback engine correctness.
Output: Updated VaenylaeBard.toc and VaenylaeBard.lua with all PORT-01/02/03/04/07 sites corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-wotlk-compatibility-port/01-RESEARCH.md
@.planning/phases/01-wotlk-compatibility-port/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: .toc Interface version bump and OnEvent/OnUpdate callback signatures + comment</name>
  <files>VaenylaeBard.toc, VaenylaeBard.lua</files>
  <read_first>
    VaenylaeBard.toc (line 1 — current: "## Interface: 11307")
    VaenylaeBard.lua (lines 134, 139, 156, 198-201 — all four sites in this task)
  </read_first>
  <action>
Make exactly these edits in order:

1. VaenylaeBard.toc, line 1:
   Before: ## Interface: 11307
   After:  ## Interface: 30300

2. VaenylaeBard.lua, line 134 — OnUpdate signature:
   Before: playbackFrame:SetScript("OnUpdate", function()
   After:  playbackFrame:SetScript("OnUpdate", function(self, elapsed)
   Note: `self` MUST be the first parameter even though the body does not use it. Writing function(elapsed) would receive the frame object as elapsed, producing garbage (astronomical) timing values that fire every line on the first frame.

3. VaenylaeBard.lua, line 139 — elapsed variable name:
   Before:     VB_playbackElapsed = VB_playbackElapsed + arg1
   After:      VB_playbackElapsed = VB_playbackElapsed + elapsed

4. VaenylaeBard.lua, line 156 — residual subtraction (PORT-07, applied while OnUpdate body is open):
   Before:         VB_playbackElapsed = 0
   After:          VB_playbackElapsed = VB_playbackElapsed - effectiveDelay

5. VaenylaeBard.lua, lines 198-199 — comment hygiene (per CONTEXT.md decision):
   Before (two lines):
     -- In WoW 1.12.1 (Lua 5.0), extra event args come through globals (arg1, arg2, ...),
     -- not function parameters. OnEvent receives (self, event) at most.
   After (two lines):
     -- In WoW 3.3.5a (Lua 5.1), extra event args are explicit function parameters.
     -- OnEvent receives (self, event, ...) where ... are event-specific args.

6. VaenylaeBard.lua, line 200 — OnEvent signature:
   Before: eventFrame:SetScript("OnEvent", function()
   After:  eventFrame:SetScript("OnEvent", function(self, event, arg1)
   Note: Parameter names `event` and `arg1` are chosen deliberately so lines 201-209 (the body) require zero further edits — the old globals are now local parameter names with identical values.

Do not modify any other lines in VaenylaeBard.lua. Do not change the OnEvent body (lines 201-209).
  </action>
  <verify>
    <automated>
      grep -n 'Interface:' /home/reset/Dev/Repos/VaenylaeBallads/VaenylaeBard.toc
      grep -n 'OnUpdate.*function' /home/reset/Dev/Repos/VaenylaeBallads/VaenylaeBard.lua
      grep -n 'playbackElapsed.*arg1' /home/reset/Dev/Repos/VaenylaeBallads/VaenylaeBard.lua
      grep -n 'playbackElapsed.*effectiveDelay' /home/reset/Dev/Repos/VaenylaeBallads/VaenylaeBard.lua
      grep -n 'OnEvent.*function' /home/reset/Dev/Repos/VaenylaeBallads/VaenylaeBard.lua
      grep -n '3\.3\.5a' /home/reset/Dev/Repos/VaenylaeBallads/VaenylaeBard.lua
    </automated>
  </verify>
  <acceptance_criteria>
    - VaenylaeBard.toc line 1 contains exactly: ## Interface: 30300
    - VaenylaeBard.lua line 134 contains: playbackFrame:SetScript("OnUpdate", function(self, elapsed)
    - VaenylaeBard.lua line 139 contains: VB_playbackElapsed = VB_playbackElapsed + elapsed (not arg1)
    - VaenylaeBard.lua line 156 contains: VB_playbackElapsed = VB_playbackElapsed - effectiveDelay
    - grep -n 'playbackElapsed.*arg1' VaenylaeBard.lua returns zero results
    - VaenylaeBard.lua lines 198-199 mention "3.3.5a" and "Lua 5.1" (not "1.12.1" or "Lua 5.0")
    - VaenylaeBard.lua line 200 contains: eventFrame:SetScript("OnEvent", function(self, event, arg1)
  </acceptance_criteria>
  <done>toc reads 30300; OnUpdate has (self, elapsed) signature and residual subtraction reset; OnEvent has (self, event, arg1) signature; comment updated to describe 3.3.5a.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all five table.getn() calls with the # operator</name>
  <files>VaenylaeBard.lua</files>
  <read_first>
    VaenylaeBard.lua (lines 91, 107, 129, 161, 630 — all five table.getn sites)
  </read_first>
  <action>
Replace every table.getn() call with the # length operator. There are exactly 5 sites:

Line 91:
  Before:     if not song or table.getn(song) == 0 then
  After:      if not song or #song == 0 then

Line 107:
  Before:     VBDebug("StartPerformance mode=" .. tostring(mode) .. " song=" .. VaenylaeBardSelectedSong .. " lines=" .. table.getn(song))
  After:      VBDebug("StartPerformance mode=" .. tostring(mode) .. " song=" .. VaenylaeBardSelectedSong .. " lines=" .. #song)

Line 129:
  Before:     if VB_playIndex > table.getn(song) then
  After:      if VB_playIndex > #song then

Line 161 (hot path — fires every frame during playback):
  Before:         if VB_playIndex > table.getn(song) then
  After:          if VB_playIndex > #song then

Line 630:
  Before:         btn:SetText(songName .. " (" .. table.getn(songData) .. " lines)")
  After:          btn:SetText(songName .. " (" .. #songData .. " lines)")

Important: The btn:SetScript("OnClick", ...) closure immediately following line 630 (lines 632-638) captures `capturedName` as an upvalue and does not use `this`. Do NOT modify that closure — it is already correct in 3.3.5a.

After making all 5 replacements, run the grep verification command to confirm zero occurrences remain.
  </action>
  <verify>
    <automated>grep -n 'table\.getn' /home/reset/Dev/Repos/VaenylaeBallads/VaenylaeBard.lua</automated>
  </verify>
  <acceptance_criteria>
    - grep -n 'table\.getn' VaenylaeBard.lua returns zero results (no output, exit 1 is fine — it means no matches)
    - Line 91 contains: if not song or #song == 0 then
    - Line 107 contains: .. " lines=" .. #song)
    - Line 129 contains: if VB_playIndex > #song then
    - Line 161 contains: if VB_playIndex > #song then
    - Line 630 contains: btn:SetText(songName .. " (" .. #songData .. " lines)")
  </acceptance_criteria>
  <done>All five table.getn() calls replaced with #; post-edit grep confirms zero remaining occurrences.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WoW API → addon Lua | WoW engine delivers events and callbacks to addon code |
| User → UI controls | Player interacts with frame buttons and text input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | VaenylaeBard.lua edit sites | accept | Source edits are deterministic single-line substitutions verified by post-edit grep; no external input involved in this plan |
| T-01-02 | Denial of Service | OnUpdate hot path (line 161) | accept | table.getn removal eliminates the risk of a nil-call error crashing the frame; # operator cannot fail on a sequence table |
| T-01-03 | Information Disclosure | SavedVariables (VaenylaeBardDB) | accept | SavedVariables are local to the WoW install, serialized by the WoW engine; no external exposure; data format unchanged by this port |

ASVS L1 requirements are satisfied by the WoW engine. No auth, no network calls beyond the WoW API, no user secrets. No additional controls needed for this plan.
</threat_model>

<verification>
Run after both tasks are complete, before merging into Plan 02's wave:

```bash
# Must return zero results:
grep -n 'table\.getn' VaenylaeBard.lua

# Must show updated values:
grep -n 'Interface:' VaenylaeBard.toc          # expect: ## Interface: 30300
grep -n 'OnEvent.*function' VaenylaeBard.lua   # expect: function(self, event, arg1)
grep -n 'OnUpdate.*function' VaenylaeBard.lua  # expect: function(self, elapsed)
grep -n 'playbackElapsed.*arg1' VaenylaeBard.lua   # expect: zero results
grep -n 'playbackElapsed.*effectiveDelay' VaenylaeBard.lua  # expect: one result (line 156)
```
</verification>

<success_criteria>
- VaenylaeBard.toc line 1 reads `## Interface: 30300`
- VaenylaeBard.lua line 134 reads `playbackFrame:SetScript("OnUpdate", function(self, elapsed)`
- VaenylaeBard.lua line 139 reads `VB_playbackElapsed = VB_playbackElapsed + elapsed`
- VaenylaeBard.lua line 156 reads `VB_playbackElapsed = VB_playbackElapsed - effectiveDelay`
- VaenylaeBard.lua lines 198-199 describe 3.3.5a/Lua 5.1 behavior
- VaenylaeBard.lua line 200 reads `eventFrame:SetScript("OnEvent", function(self, event, arg1)`
- `grep -n 'table\.getn' VaenylaeBard.lua` returns zero results
- `grep -n 'playbackElapsed.*arg1' VaenylaeBard.lua` returns zero results
</success_criteria>

<output>
After completion, create `.planning/phases/01-wotlk-compatibility-port/01-01-SUMMARY.md`
</output>
